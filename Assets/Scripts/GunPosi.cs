using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GunPosi : MonoBehaviour
{
    //float liftSpeed = 10;
    //GameObject target;
    //GameObject target2;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
        if (Input.GetButton("Crouch"))
        {
            //transform.position = Vector2.Lerp(transform.position, target2.transform.position, Time.deltaTime * liftSpeed);
            Vector2 pos = transform.position;
            pos.y = -3.8f;
            transform.position = pos;
        }
        else if (Input.GetButtonUp("Crouch"))
        {
            //transform.position = Vector2.Lerp(transform.position, target.transform.position, Time.deltaTime * liftSpeed);
            Vector2 pos = transform.position;
            pos.y = -3f;
            transform.position = pos;
        }
        */
    }
}
